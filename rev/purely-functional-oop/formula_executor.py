# Executes a google sheets formula and returns the result.
# If you don't have API access, just copy and paste into your own file

import requests as r

# the implementation of this function will be censored during the challenge
# to prevent DOS of our infrastructure.
def execute_formula(formula):
    #url = "https://script.google.com/macros/s/URL_REDACTED/exec"
    #resp = r.post(url, data=formula)
    print(f"\n\n===============\n\n{formula}\n\n===============\n\n")

    # return resp.text
    return "please run the printed formula to know the actual result."
